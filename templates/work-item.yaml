wi_id: __WI_ID__
goal: [one execution goal]
scope:
  - [what this work item may change]
out_of_scope:
  - [what must not be added]

allowed_paths:
  - src/**
  - change/__CONTAINER__/testing.md
forbidden_paths:
  - versions/**

branch_execution:
  container: __CONTAINER__
  execution_branch: group/__CONTAINER__
  owned_paths:
    - src/[owned-slice]/**
  shared_paths:
    - src/[shared-surface]
  shared_file_owner: parent-feature
  merge_order: [relative merge order]
  conflict_policy: stop and update design before expanding shared scope

phase_scope: Implementation

input_refs:
  - docs/inputs/example.md#intent
requirement_refs:
  - REQ-001
acceptance_refs:
  - ACC-001
verification_refs:
  - VO-001
# keep acceptance_refs aligned with design.md Work Item Derivation.covered_acceptance_refs
# keep requirement_refs / verification_refs aligned with design.md Goal / Scope Link and Work Item Derivation
derived_from: __DERIVED_FROM__
dependency_refs: []
dependency_type: strong
contract_refs: []

evidence_refs: []

verification_profile: focused
required_verification:
  - [test or verification required before this WI is considered complete]

stop_conditions:
  - [when the session must stop and ask]
reopen_triggers:
  - [when spec/design must be reopened]
hard_constraints:
  - [non-negotiable constraint]
